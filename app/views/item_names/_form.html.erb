<%= form_for(@item_name) do |f| %>
  <% if @item_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_name.errors.count, "error") %> prohibited this item_name from being saved:</h2>

      <ul>
      <% @item_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
